# Repo Map — co2-eos

> Generated by `repo-outline` — do not edit by hand. Run `repo-outline --write` to refresh.
> This inventory is derived from the source on disk; it is the navigation source of truth. CLAUDE.md carries the stable where-to-write / source-of-truth guidance, this carries the literal file map.

## Python

### bench/compare.py
_Before/after benchmark of the EOS hot path: original autodiff vs new analytic._
- const NS  ·L24
- const SUPER  ·L25
- regime(n, seed=0)  ·L28
- timed(fn, *args, repeat=50)  ·L37
- @jax.jit before_invert(rho, u, phase)  ·L48
- @jax.jit before_props(T, rho)  ·L52
- @jax.jit before_full(rho, u, phase)  ·L56
- main()  ·L69

### bench/fixed_iters_study.py
_Round-trip T error vs FIXED Newton iteration count, for candidate seeds._
- make_points(Tlo, Thi, rlo, rhi, n=4000, seed=1)  ·L17
- solve_fixed(rho, u, T0_fn, niters)  ·L26
- const SEEDS  ·L37
- main()  ·L44

### bench/profile_baseline.py
_Baseline profiling of the co2-eos hot path (pre-redesign)._
- const NS  ·L36
- const REPEAT  ·L37
- regime_grid(n, seed=0)  ·L43 — Return (T, rho, u, phase) arrays of length n sampled in the regime.
- time_call(fn, *args, repeat=REPEAT)  ·L57 — Compile, warm up, then time `fn(*args)` over `repeat` runs.
- @jax.jit consumer_hot_path(rho, u, phase)  ·L77
- _count_one(rho, u)  ·L92
- newton_iteration_counts(T, rho, u)  ·L117
- main()  ·L121

### bench/profile_hotpath.py
_Profile INSIDE properties_from_rho_u: where the per-eval microseconds go._
- const NS  ·L44
- const REPEAT  ·L45
- envelope(n, seed=0)  ·L51 — (T, rho, u) sampled uniformly in the consumers' operating box.
- time_call(fn, *args, repeat=REPEAT)  ·L60 — Compile + warm up, then median seconds/call over `repeat` runs.
- @jax.jit f_bundle(tau, delta)  ·L83
- const _TAU_REF  ·L87
- @jax.jit f_chi_ref(delta)  ·L91 — Exactly the extra Helmholtz work the critical enhancement does.
- const _OPS  ·L98
- hlo_census(fn, *args)  ·L102 — Count transcendental-ish ops in the optimized HLO of fn(*args).
- newton_convergence(n=20000, seed=7)  ·L114 — |T_k - T_true| percentiles after k fixed Newton steps from the table seed.
- main()  ·L144

### bench/proto_chebsurf.py
_Prototype D probe: can a tensor-Chebyshev surface replace the residual sum?_
- const C  ·L29
- cheb_nodes(n, lo, hi)  ·L32
- alphar_parts(tau, delta)  ·L37 — (full alpha_r, smooth part = poly+exp+gauss only) and their tt/dd.
- fit_and_test(values_fn, label, degrees=(20, 40, 60, 80))  ·L61
- main()  ·L107

### bench/proto_econ.py
_Prototype A/B: transcendental-economized Span-Wagner kernels, measured._
- const _N  ·L41
- const _D  ·L42
- const _T  ·L43
- const _L  ·L44
- const _GN  ·L46
- const _GD  ·L47
- const _GT  ·L48
- const _GETA  ·L49
- const _GBETA  ·L50
- const _GGAMMA  ·L51
- const _GEPS  ·L52
- const _NAN_  ·L54
- const _NAA  ·L55
- const _NAB  ·L56
- const _NA_P  ·L57
- const _NA_BIGA  ·L59
- const _NA_BIGB  ·L60
- const _NA_BIGC  ·L61
- const _NA_BIGD  ·L62
- const _TPOWS  ·L70
- _tau_ladder(tau)  ·L75 — All distinct tau^t (t in _TPOWS) via 2 sqrts + multiplies.
- _delta_ladder(delta, dmax)  ·L109 — delta^1..dmax by successive multiplication (dict of int -> value).
- residual_derivs_econ(tau, delta)  ·L121 — (ar, ar_d, ar_t, ar_dd, ar_tt, ar_dt) — exact SW96, economized.
- const _TPOLY  ·L264
- const _TP_IDX  ·L265
- const _W_T  ·L266
- const _W_TT  ·L267
- residual_tau_prep_econ(delta)  ·L270 — delta-invariant state for the tau-only inner loop (economized).
- residual_tau_fast_econ(tau, dstate)  ·L299 — (ar_t, ar_tt) from precomputed delta-state — economized per-iteration.
- const _TAU_REF  ·L373
- _dpdrho_ref_exact(delta)  ·L377
- fit_chi_ref_cheb(deg)  ·L382
- chi_ref_cheb(delta, coefs)  ·L391 — Clenshaw evaluation of the fitted dp/drho|_(T_ref) factor.
- _grid(n=300)  ·L405 — (tau, delta) covering envelope + margins incl. tight near-critical.
- time_call(fn, *args, repeat=200)  ·L421
- main()  ·L432

### bench/proto_seed_v2.py
_Prototype C: seed-table v2 — dome-safe fill + denser grid -> fewer iters._
- const NU  ·L34
- build_v2()  ·L37
- make_seed_fn(rho_grid, u_grid, T0)  ·L77
- conv_stats(seed_fn, label, n=40000)  ·L98
- main()  ·L128

### bench/seed_study.py
_Pick the (ρ, u) → T Newton seed: compare seed error and iteration counts._
- make_points(Tlo, Thi, rlo, rhi, n=2000, seed=0)  ·L23
- seed_crude(rho, u)  ·L32
- seed_const(rho, u)  ·L36
- _ideal_u(T)  ·L40
- seed_ideal(rho, u)  ·L48 — Invert ideal-gas u_id(T)=u with a few fixed Newton steps from 300 K.
- newton_iters_to_conv(rho, u, T0)  ·L61 — Count analytic-Newton iterations until |step| < 1e-10 K (cap 40).
- report(name, T_true, rho, u, T0)  ·L79
- main()  ·L88

### bench/seed_table_study.py
_Does a precomputed T0(rho,u) table seed collapse the near-critical iters?_
- u_of(T, rho)  ·L11
- make_points(Tlo, Thi, rlo, rhi, n=6000, seed=3)  ·L14
- build_table()  ·L25
- table_seed(rho, u)  ·L45
- affine_seed(rho, u)  ·L61
- solve_fixed(rho, u, seed_fn, n)  ·L64
- main()  ·L74

### bench/validate_analytic.py
_Validate the hand-coded analytic Helmholtz derivatives against jax.grad._
- grid()  ·L30 — (tau, delta) covering the near-critical regime and beyond.
- main()  ·L43

### bench/validate_core.py
_Validate the redesigned core: round-trip, properties, seed/iteration count._
- pts(Tlo, Thi, rlo, rhi, n, seed)  ·L7
- roundtrip(label, Tlo, Thi, rlo, rhi, n=8000, seed=11)  ·L15
- seed_quality(Tlo, Thi, rlo, rhi, n=8000, seed=5)  ·L25
- properties()  ·L33
- main()  ·L48

### bench/validate_fastpath.py
_Validation harness: the fast (ρ, u) path vs an independent reference chain._
- _T_from_rho_u_ref(rho, u)  ·L45 — To-convergence Newton on the oracle u(T, ρ); crude seed, no table.
- const _A0  ·L66
- const _RF_B  ·L69
- const _RF_T  ·L71
- const _LAM0_L  ·L72
- const _LAM_B  ·L73
- const _M  ·L77
- const _R_MOLAR  ·L78
- const _RHOC_MOLAR  ·L79
- const _PC  ·L80
- const _TT_TRIPLE  ·L81
- const _RHO_TL  ·L82
- const _ETA_TL  ·L83
- _visc_ref(T, rho)  ·L86
- _cond_ref(T, rho, mu)  ·L104
- reference_state(rho, u)  ·L143 — Full reference state dict at scalar (ρ, u) from the oracle chain.
- reference_derivs(rho, u, qname)  ·L164
- envelope_single_phase(n, seed=17)  ·L188
- const QUANTITIES  ·L210
- rel(a, b, floor)  ·L214
- const FLOORS  ·L221
- main()  ·L226

### co2_eos/__init__.py
_CO2-EOS: Differentiable CO₂ thermodynamic properties in JAX._
- const LIQUID  ·L58
- const VAPOR  ·L59
- const SUPERCRITICAL  ·L60
- const AUTO  ·L61
- @jax.jit viscosity(T, rho)  ·L84 — Dynamic viscosity μ [Pa·s] at scalar (T, ρ). vmap for batches.
- @jax.jit thermal_conductivity(T, rho)  ·L92 — Thermal conductivity λ [W/(m·K)] at scalar (T, ρ). vmap for batches.
- @jax.jit properties_from_rho_u(rho, u, phase_hint=SUPERCRITICAL)  ·L104 — Full thermodynamic + transport state from (ρ, u). Batched.
- @jax.jit temperature_from_rho_u(rho, u, phase_hint=SUPERCRITICAL)  ·L133 — Temperature T [K] such that u(T,ρ)=u at fixed ρ. Batched.
- @jax.jit properties(T, rho)  ·L151 — Full thermodynamic + transport state at scalar (T, ρ).
- @jax.jit state_from_PT(P, T, phase_hint=AUTO)  ·L162 — Full state at (P, T). Phase-aware Halley solve for ρ, then derive all.
- @jax.jit density_from_PT(P, T, phase_hint=AUTO)  ·L177 — Density [kg/m³] at scalar (P, T). Phase-aware Halley solve.
- @jax.jit state_from_Ph(P, h, phase_hint=AUTO)  ·L186 — Full state at (P, h). Dome-aware at subcritical P; else 2-D Newton.
- @jax.jit state_from_Du(rho, u, phase_hint=AUTO)  ·L202 — Full state at scalar (ρ, u). Scalar form of ``properties_from_rho_u``.

### co2_eos/core.py
_Redesigned high-performance EOS core — analytic derivatives, fused passes._
- const _NEWTON_ITERS_TABLE  ·L48
- const _NEWTON_ITERS_AFFINE  ·L49
- const _T_MIN  ·L50
- const _T_MAX  ·L51
- const _SEED_PATH  ·L62
- const _HAVE_SEED  ·L66
- _seed_T(rho, u)  ·L91 — Initial T guess for the (ρ, u) inversion via the bilinear table.
- _thermo(T, rho)  ·L122 — All eight Helmholtz-derived properties at scalar (T, ρ).
- _u_and_cv(T, rho)  ·L153 — Internal energy u [J/kg] and Cv [J/(kg·K)] at scalar (T, ρ).
- _solve_T(rho, u)  ·L172 — Fixed-iteration Newton solve for T given (ρ, u). Scalar.
- @jax.custom_jvp _temperature_from_Du(rho, u, phase_hint)  ·L198 — T such that u(T, ρ) = u_target at fixed ρ. Scalar. (phase_hint unused.)
- @_temperature_from_Du.defjvp _temperature_from_Du_jvp(primals, tangents)  ·L204 — dT = (du − (∂u/∂ρ)·dρ) / Cv, with Cv and ∂u/∂ρ analytic.
- _state_from_T_rho(T, rho)  ·L230 — Scalar fused state at (T, ρ): every thermo + transport property.
- _state_from_rho_u(rho, u, phase_hint)  ·L262 — Scalar fused state from (ρ, u): solve T once, derive everything.

### co2_eos/helmholtz.py
_Span-Wagner (1996) reduced Helmholtz energy with hand-coded analytic_
- const _PN  ·L50
- const _PD  ·L51
- const _PT  ·L52
- const _PL  ·L53
- const _GN  ·L55
- const _GD  ·L56
- const _GT  ·L57
- const _GETA  ·L58
- const _GBETA  ·L59
- const _GGAMMA  ·L60
- const _NAN  ·L62
- const _NAA  ·L63
- const _NAB  ·L64
- const _NABIGB  ·L65
- const _NABIGC  ·L66
- const _NA_P_SCALAR  ·L74
- const _NA_BIGA_SCALAR  ·L76
- const _NA_BIGD_SCALAR  ·L78
- const _NA_P  ·L85
- const _TPOLY  ·L89
- const _TP_IDX  ·L90
- const _TALL  ·L91
- const _W_T  ·L92
- const _W_TT  ·L93
- _tau_ladder(tau)  ·L96 — All distinct τ^t needed by the terms, via 2 sqrts + multiplies.
- _delta_ladder(delta, dmax=10)  ·L128 — δ^0..δ^dmax by successive multiplication.
- _exp_envelopes(dp)  ·L136 — The six distinct exponential envelopes E_l = exp(-δ^l), l = 0..6.
- ideal_derivs(tau, delta)  ·L148 — Return (α⁰, α⁰_τ, α⁰_ττ) at scalar (τ, δ).
- ideal_tau_only(tau)  ·L174 — Return (α⁰_τ, α⁰_ττ) — the only ideal quantities the Newton needs.
- _pow78(Db)  ·L193 — Db^(7/8) by three sqrts and three multiplies (Db ≥ 1e-300 guarded).
- _na_delta_state(delta)  ·L201 — δ-side quantities of the non-analytic terms (loop-invariant at fixed ρ).
- residual_derivs(tau, delta)  ·L226 — Return (αʳ, αʳ_δ, αʳ_τ, αʳ_δδ, αʳ_ττ, αʳ_δτ) at scalar (τ, δ).
- residual_tau_prep(delta)  ·L353 — Precompute the δ-invariant state for the τ-derivative inner loop.
- residual_tau_fast(tau, dstate)  ·L377 — Return (αʳ_τ, αʳ_ττ) from precomputed δ-state ``dstate``.
- residual_tau_derivs(tau, delta)  ·L443 — Return (αʳ_τ, αʳ_ττ) — the residual quantities the Newton needs.

### co2_eos/inversions.py
_Thermodynamic inversions — pure JAX, JIT-compilable, vmappable,_
- const LIQUID  ·L35
- const VAPOR  ·L36
- const SUPERCRITICAL  ·L37
- const MAX_ITER  ·L40
- const TOL  ·L41
- _initial_guess(T, P, phase_hint)  ·L57 — Return an initial density guess based on phase_hint.
- _safe_denom(x, eps=1e-30)  ·L102 — Guard a denominator: preserve sign for small values, fall back to +eps for NaN.
- _halley_body(state)  ·L112 — One Halley step (Householder order 2) for cubic convergence.
- _halley_cond(state)  ·L136 — Continue while not converged and under max iterations.
- const _BISECT_LO  ·L144
- const _BISECT_HI  ·L145
- const _BISECT_ITERS  ·L146
- _bisection_body(state)  ·L149 — One bisection step on f(ρ) = P(T,ρ) − P_target.
- _bisection_cond(state)  ·L168
- _bisection_solve(T, P)  ·L173 — Bisection solve for ρ given (T, P). Scalar inputs. Bounds [1, 1200] kg/m³.
- _solve_density(T, P, phase_hint)  ·L181 — Halley solve for ρ given (T, P, phase_hint), with bisection fallback. Scalar inputs.
- @jax.custom_jvp _density_from_PT(T, P, phase_hint)  ·L206 — Find ρ such that P(T,ρ) = P_target. Scalar inputs.
- @_density_from_PT.defjvp _density_from_PT_jvp(primals, tangents)  ·L221 — JVP via implicit function theorem.
- @jax.jit density_from_PT(T, P, phase_hint)  ·L243 — Find ρ [kg/m³] such that P(T,ρ) = P_target.
- _initial_guess_Ph(P, h, phase_hint)  ·L268 — Estimate (T, ρ) from (P, h) for the 2D Newton solver.
- _newton_body_2d(state)  ·L337 — One 2D Newton step on F = [P(T,ρ) - P_target, h(T,ρ) - h_target].
- _newton_cond_2d(state)  ·L377 — Continue while not converged and under max iterations.
- _solve_state_Ph(P, h, phase_hint)  ·L383 — 2D Newton solve for (T, ρ) given (P, h, phase_hint). Scalar inputs.
- @jax.custom_jvp _state_from_Ph(P, h, phase_hint)  ·L396 — Find (T, ρ) such that P(T,ρ) = P and h(T,ρ) = h. Scalar inputs.
- @_state_from_Ph.defjvp _state_from_Ph_jvp(primals, tangents)  ·L438 — JVP via implicit function theorem on F(T,ρ; P,h) = 0.
- @jax.jit state_from_Ph(P, h, phase_hint)  ·L500 — Find (T [K], ρ [kg/m³]) such that P(T,ρ) = P and h(T,ρ) = h.
- _initial_guess_Du(rho, u)  ·L525 — Estimate T from (ρ, u) using u ≈ Cv_approx * R * T.
- _newton_body_Du(state)  ·L536 — One Newton step: T_{n+1} = T_n - f(T)/f'(T) where f = u(T,ρ) - u_target.
- _newton_cond_Du(state)  ·L551 — Continue while not converged and under max iterations.
- _solve_temperature_Du(rho, u, phase_hint)  ·L557 — Newton solve for T given (ρ, u, phase_hint). Scalar inputs.
- @jax.custom_jvp _temperature_from_Du(rho, u, phase_hint)  ·L569 — Find T such that u(T, ρ) = u_target at fixed ρ. Scalar inputs.
- @_temperature_from_Du.defjvp _temperature_from_Du_jvp(primals, tangents)  ·L584 — JVP via implicit function theorem.
- @jax.jit temperature_from_Du(rho, u, phase_hint)  ·L606 — Find T [K] such that u(T, ρ) = u_target at fixed ρ.
- @jax.custom_jvp _state_from_Du(rho, u, phase_hint)  ·L628 — Find (T, P, h) given (ρ, u). Scalar inputs.
- @_state_from_Du.defjvp _state_from_Du_jvp(primals, tangents)  ·L648 — JVP via implicit differentiation + chain rule.
- @jax.jit state_from_Du(rho, u, phase_hint)  ·L676 — Find (T [K], P [Pa], h [J/kg]) given (ρ, u) at fixed density.

### co2_eos/saturation.py
_CO2 saturation curve lookups — pure JAX, JIT-compilable._
- const _TABLE_PATH  ·L22
- const _TABLE  ·L28
- _ensure_loaded()  ·L31 — Load saturation table on first use. Must be called outside JIT.
- _eval_spline(x, breaks, coeffs)  ·L42 — Evaluate cubic spline at x.
- saturation_pressure(T)  ·L66 — Saturation pressure P_sat [Pa] at temperature T [K].
- saturation_densities(T)  ·L76 — Saturated liquid and vapor densities [kg/m3] at temperature T [K].
- saturation_enthalpies_T(T)  ·L88 — Saturated liquid and vapor enthalpies [J/kg] at temperature T [K].
- saturation_entropies(T)  ·L100 — Saturated liquid and vapor entropies [J/(kg*K)] at temperature T [K].
- saturation_temperature(P)  ·L114 — Saturation temperature T_sat [K] at pressure P [Pa].
- saturation_enthalpies(P)  ·L123 — Saturated liquid and vapor enthalpies [J/kg] at pressure P [Pa].
- saturation_densities_P(P)  ·L135 — Saturated liquid and vapor densities [kg/m3] at pressure P [Pa].
- saturation_entropies_P(P)  ·L147 — Saturated liquid and vapor entropies [J/(kg*K)] at pressure P [Pa].

### co2_eos/span_wagner.py
_Span-Wagner (1996) equation of state for CO₂ — pure JAX implementation._
- const TC  ·L21
- const RHOC_MOLAR  ·L22
- const M  ·L23
- const RHOC  ·L24
- const R_MOLAR  ·L25
- const R  ·L26
- const PC  ·L27
- const T_TRIPLE  ·L28
- const _A0_A1  ·L43
- const _A0_A2  ·L44
- const _A0_LOGTAU  ·L45
- const _A0_PE_N  ·L47
- const _A0_PE_T  ·L49
- alpha0(tau, delta)  ·L52 — Ideal-gas reduced Helmholtz energy α⁰(τ, δ).
- const _AR_N  ·L76
- const _AR_D  ·L91
- const _AR_T  ·L99
- const _AR_L  ·L107
- const _AR_GAUSS_N  ·L117
- const _AR_GAUSS_D  ·L122
- const _AR_GAUSS_T  ·L123
- const _AR_GAUSS_ETA  ·L125
- const _AR_GAUSS_BETA  ·L126
- const _AR_GAUSS_GAMMA  ·L127
- const _AR_GAUSS_EPS  ·L128
- const _NA_N  ·L132
- const _NA_A  ·L133
- const _NA_B  ·L134
- const _NA_BETA  ·L135
- const _NA_BIG_A  ·L136
- const _NA_BIG_B  ·L137
- const _NA_BIG_C  ·L138
- const _NA_BIG_D  ·L139
- alphar(tau, delta)  ·L142 — Residual reduced Helmholtz energy αʳ(τ, δ).
- alpha(tau, delta)  ·L188 — Total reduced Helmholtz energy α(τ, δ) = α⁰ + αʳ.
- _alpha_of_T_rho(T, rho)  ·L208 — α(τ(T), δ(ρ)) as a function of physical T, ρ — for autodiff.
- _alphar_of_T_rho(T, rho)  ·L215 — αʳ(τ(T), δ(ρ)) as a function of physical T, ρ.
- _pressure_reduced(tau, delta)  ·L237 — P / (ρ·R·T) = 1 + δ·αʳ_δ.
- _cv_reduced(tau, delta)  ·L242 — Cv / R = -τ²·(α⁰_ττ + αʳ_ττ).
- _cp_reduced(tau, delta)  ·L247 — Cp / R = Cv/R + (1 + δ·αʳ_δ - δ·τ·αʳ_δτ)² / (1 + 2δ·αʳ_δ + δ²·αʳ_δδ).
- _speed_of_sound_sq_reduced(tau, delta)  ·L259 — w² / (R·T) = 1 + 2δ·αʳ_δ + δ²·αʳ_δδ
- _scalar_pressure(T, rho)  ·L278 — Pressure [Pa] at scalar (T, ρ).
- _scalar_cv(T, rho)  ·L285 — Isochoric heat capacity Cv [J/(kg·K)] at scalar (T, ρ).
- _scalar_cp(T, rho)  ·L292 — Isobaric heat capacity Cp [J/(kg·K)] at scalar (T, ρ).
- _scalar_speed_of_sound(T, rho)  ·L299 — Speed of sound [m/s] at scalar (T, ρ).
- _scalar_enthalpy(T, rho)  ·L307 — Specific enthalpy h [J/kg] at scalar (T, ρ).
- _scalar_internal_energy(T, rho)  ·L320 — Specific internal energy u [J/kg] at scalar (T, ρ).
- _scalar_entropy(T, rho)  ·L332 — Specific entropy s [J/(kg·K)] at scalar (T, ρ).
- @jax.jit pressure(T, rho)  ·L351 — Pressure [Pa]. Batched over leading axis.
- @jax.jit cv(T, rho)  ·L357 — Isochoric heat capacity Cv [J/(kg·K)]. Batched.
- @jax.jit cp(T, rho)  ·L363 — Isobaric heat capacity Cp [J/(kg·K)]. Batched.
- @jax.jit speed_of_sound(T, rho)  ·L369 — Speed of sound [m/s]. Batched.
- @jax.jit enthalpy(T, rho)  ·L375 — Specific enthalpy h [J/kg]. Batched.
- @jax.jit internal_energy(T, rho)  ·L381 — Specific internal energy u [J/kg]. Batched.
- @jax.jit entropy(T, rho)  ·L387 — Specific entropy s [J/(kg·K)]. Batched.
- @jax.jit all_properties(T, rho)  ·L393 — Compute all properties at once. Returns dict.
- _scalar_gibbs_energy(T, rho)  ·L412 — Specific Gibbs energy g = h - T·s [J/kg] at scalar (T, ρ).
- @jax.jit gibbs_energy(T, rho)  ·L426 — Specific Gibbs energy g [J/kg]. Batched.
- _scalar_all_properties_shared(T, rho)  ·L435 — Compute all 7 properties + Gibbs energy from 5 shared reduced derivatives.
- @jax.jit all_properties_shared(T, rho)  ·L491 — Compute all properties from shared reduced derivatives. Returns dict.

### co2_eos/transport.py
_CO₂ transport property correlations — pure JAX implementation._
- const TC  ·L21
- const RHOC_MOLAR  ·L22
- const M  ·L23
- const R_MOLAR  ·L24
- const PC  ·L25
- const RHOC_MASS  ·L26
- const _ETA0_A  ·L43
- _eta_dilute(T)  ·L54 — Dilute-gas viscosity η₀(T) [Pa·s].
- const _EPSILON_OVER_K  ·L72
- const _SIGMA  ·L73
- const _NA  ·L74
- const _RF_B  ·L76
- const _RF_T  ·L80
- _eta_initial(T, rho_molar, eta0)  ·L83 — Initial-density viscosity contribution [Pa·s].
- const _TT  ·L106
- const _RHO_TL  ·L107
- const _C1  ·L108
- const _C2  ·L109
- const _GAMMA_VISC  ·L110
- const _ETA_TL  ·L113
- _eta_residual(T, rho_mass)  ·L118 — Higher-order viscosity contribution [Pa·s].
- _scalar_viscosity(T, rho)  ·L129 — Dynamic viscosity μ [Pa·s] at scalar (T, ρ [kg/m³]).
- const _LAM0_L  ·L143
- _lambda_dilute(T)  ·L146 — Dilute-gas thermal conductivity λ₀(T) [W/(m·K)].
- const _LAM_RES_B  ·L156
- const _LAM_RES_D  ·L160
- const _LAM_RES_T  ·L161
- const _RHOMASS_REDUCING  ·L163
- _lambda_residual(T, rho)  ·L166 — Residual thermal conductivity [W/(m·K)].
- const _KB  ·L183
- const _R0  ·L184
- const _GAMMA_CE  ·L185
- const _GAMMA_EXP  ·L186
- const _NU  ·L187
- const _ZETA0  ·L188
- const _QD  ·L189
- const _T_REF  ·L190
- const _CHI_PATH  ·L198
- const _HAVE_CHI_CHEB  ·L199
- _dpdrho_ref_reduced(delta)  ·L214 — 1 + 2δ·αʳ_δ + δ²·αʳ_δδ at T_ref: Chebyshev-Clenshaw (or exact fallback).
- _lambda_critical_shared(T, rho, mu, alr_d, alr_dd, alr_tt, alr_dt, al0_tt)  ·L233 — Critical enhancement of thermal conductivity [W/(m·K)].
- _thermal_conductivity_shared(T, rho, mu, alr_d, alr_dd, alr_tt, alr_dt, al0_tt)  ·L298 — λ [W/(m·K)] reusing reduced derivatives already computed at (T, ρ).
- _scalar_thermal_conductivity(T, rho)  ·L312 — Thermal conductivity λ [W/(m·K)] at scalar (T, ρ [kg/m³]).
- @jax.jit viscosity(T, rho)  ·L328 — Dynamic viscosity μ [Pa·s]. Batched over leading axis.
- @jax.jit thermal_conductivity(T, rho)  ·L342 — Thermal conductivity λ [W/(m·K)]. Batched over leading axis.

### examples/readme_check.py
_Run every Python snippet from README.md and print results._
- _check(label, ok, detail='')  ·L23
- block_properties_from_T_rho()  ·L30
- block_state_from_PT()  ·L44
- block_jax_grad()  ·L61
- block_jax_vmap()  ·L72
- block_dir_check()  ·L87
- main()  ·L103

### scripts/generate_chi_ref_table.py
_Generate the chi_ref Chebyshev table for the thermal-conductivity_
- const T_REF  ·L30
- const DEGREE  ·L32
- const OUT  ·L34
- f_exact(delta)  ·L37
- main()  ·L43

### scripts/generate_saturation_table.py
_Generate CO2 saturation table from Span-Wagner EOS._
- const T_TRIPLE  ·L30
- const T_CRIT  ·L31
- const T_NEAR_CRIT  ·L32
- const DATA_DIR  ·L34
- gibbs(T, rho)  ·L37 — Specific Gibbs energy g = h - T*s [J/kg].
- maxwell_residual(log_rhos, T)  ·L44 — Residual for Maxwell construction in log-density space.
- build_temperature_grid(n_bulk=300, n_near=300, n_vnear=200)  ·L65 — Non-uniform T grid: bulk + near-critical + very-near-critical.
- solve_saturation_curve(T_grid)  ·L81 — Solve Maxwell construction at each temperature.
- fit_and_save(T_grid, P_sat, rho_l, rho_v, h_l, h_v, s_l, s_v)  ·L142 — Fit cubic splines and save coefficients to npz.
- main()  ·L174

### scripts/generate_seed_table.py
_Generate the (ρ, u) → T Newton seed table for the inversion hot path._
- const NU  ·L40
- const OUT  ·L42
- _u_cv(T, rho)  ·L45
- _solve_T(rho, u)  ·L49 — Robust offline solve: damped Newton from the supercritical side.
- main()  ·L67

### tests/__init__.py
- _(no top-level symbols)_

### tests/test_accuracy_vs_coolprop.py
_Headline accuracy test against CoolProp._
- const CP  ·L32
- const _CP_KEYS  ·L38
- const _DEFAULT_RTOL  ·L53
- const _RTOL_OVERRIDES  ·L55
- _supercritical_grid()  ·L78
- _subcritical_liquid_grid()  ·L84
- _subcritical_vapor_grid()  ·L93
- const REGIONS  ·L102
- @pytest.fixture region_data(request)  ·L112 — Compute co2_eos and CoolProp values once per region.
- @pytest.mark.parametrize test_matches_coolprop(region_data, quantity)  ·L135 — Per (region, quantity), every grid point agrees with CoolProp to rtol.

### tests/test_analytic_derivs.py
_The hand-coded analytic Helmholtz derivatives must match jax.grad._
- _grid()  ·L29
- const PTS  ·L40
- _rel(a, b)  ·L43
- test_residual_derivs_match_autodiff()  ·L47
- test_ideal_derivs_match_autodiff()  ·L64
- test_fast_tau_path_matches_full()  ·L78 — The δ-precomputed Newton inner-loop path equals the full bundle.

### tests/test_dome_detection.py
_Tests for two-phase dome detection in state_from_Ph._
- const P_TEST  ·L26
- @pytest.fixture dome_data()  ·L30 — Precompute saturation data at P_TEST.
- class TestDomeSweepLiquid  ·L44 — Enthalpy sweep at P=7.1 MPa with LIQUID hint.
  - test_all_finite(self, dome_data)  ·L47 — All returned T and ρ are finite across h_l-50kJ to h_v+50kJ.
  - test_T_equals_Tsat_inside_dome(self, dome_data)  ·L61 — Inside dome, T should equal T_sat(P).
  - test_rho_equals_rho_l_inside_dome(self, dome_data)  ·L73 — Inside dome with LIQUID hint, ρ should equal ρ_l(P).
- class TestDomeSweepVapor  ·L86 — Enthalpy sweep at P=7.1 MPa with VAPOR hint.
  - test_all_finite(self, dome_data)  ·L89 — All returned T and ρ are finite across h_l-50kJ to h_v+50kJ.
  - test_rho_equals_rho_v_inside_dome(self, dome_data)  ·L103 — Inside dome with VAPOR hint, ρ should equal ρ_v(P).
- class TestDomeCpPositive  ·L116 — Cp computed from dome-returned (T, ρ) must be positive.
  - test_cp_positive_liquid(self, dome_data)  ·L119 — Cp > 0 across entire sweep with LIQUID hint.
  - test_cp_positive_vapor(self, dome_data)  ·L134 — Cp > 0 across entire sweep with VAPOR hint.
- class TestDomeGradients  ·L150 — Gradients through state_from_Ph at a point inside the dome.
  - test_grad_finite_inside_dome(self, dome_data)  ·L153 — jax.grad returns finite values for a point inside the dome.
  - test_jvp_finite_inside_dome(self, dome_data)  ·L177 — JVP returns finite tangents for a point inside the dome.
- class TestDomeSupercriticalBypass  ·L191 — At supercritical P, dome detection should not activate.
  - test_supercritical_unchanged(self)  ·L194 — state_from_Ph at P > PC should behave exactly as before.

### tests/test_fastpath_validation.py
_The economized (ρ, u) fast path must match the independent oracle chain._
- const _REPO  ·L24
- @pytest.fixture envelope()  ·L32
- test_properties_vs_oracle(envelope)  ·L37 — Every property ≤ 5e-10 rel vs the independent chain (measured ~1e-10).
- test_derivatives_vs_ift_oracle(envelope)  ·L47 — d(q)/d(ρ,u), forward and reverse, ≤ 1e-9 vs IFT-oracle references.
- test_chi_ref_cheb_matches_exact_bundle()  ·L72 — The precomputed Chebyshev equals the exact bundle at τ_ref ≤ 5e-12.
- test_inversion_roundoff_at_three_iterations(envelope)  ·L87 — 3 fixed Newton steps from the v2 seed reach ≤ 1e-11 K single-phase.

### tests/test_inversion_gradients.py
_Forward-mode (jvp) and reverse-mode (grad/vjp) gradients through every inversion._
- finite_diff(f, x, idx, rel_eps=1e-07)  ·L32 — Central finite difference of f with respect to x[idx].
- assert_close(a, b, rtol=0.0001, atol=1e-08, name='')  ·L51 — Assert two values are close, with helpful error message.
- const T_SC  ·L66
- const P_SC  ·L67
- const HINT_SC  ·L68
- const T_LIQ  ·L71
- const P_LIQ  ·L72
- const HINT_LIQ  ·L73
- const T_VAP  ·L76
- const P_VAP  ·L77
- const HINT_VAP  ·L78
- class TestDensityFromPT  ·L85 — Forward and reverse AD through density_from_PT.
  - @pytest.fixture test_point(self, request)  ·L93
  - test_jvp_finite(self, test_point)  ·L97
  - test_grad_finite(self, test_point)  ·L106
  - test_jvp_matches_fd(self, test_point)  ·L113
  - test_vjp_matches_fd(self, test_point)  ·L127
- class TestStateFromPh  ·L143 — Forward and reverse AD through state_from_Ph.
  - @pytest.fixture test_point(self, request)  ·L151
  - test_jvp_finite(self, test_point)  ·L161
  - test_grad_finite(self, test_point)  ·L170
  - test_jvp_matches_fd(self, test_point)  ·L179
  - test_vjp_matches_fd(self, test_point)  ·L195
- class TestTemperatureFromDu  ·L213 — Forward and reverse AD through temperature_from_Du.
  - @pytest.fixture test_point(self, request)  ·L220
  - test_jvp_finite(self, test_point)  ·L227
  - test_grad_finite(self, test_point)  ·L236
  - test_jvp_matches_fd(self, test_point)  ·L243
  - test_vjp_matches_fd(self, test_point)  ·L253
- class TestStateFromDu  ·L267 — Forward and reverse AD through state_from_Du.
  - @pytest.fixture test_point(self, request)  ·L274
  - test_jvp_finite(self, test_point)  ·L281
  - test_grad_finite(self, test_point)  ·L290
  - test_jvp_matches_fd(self, test_point)  ·L299
  - test_vjp_matches_fd(self, test_point)  ·L317

### tests/test_map_fresh.py
_Guard: docs/MAP.md stays in sync with the source._
- const REPO_ROOT  ·L14
- test_map_is_fresh()  ·L17

### tests/test_no_coolprop_runtime.py
_Discipline test: the runtime package must not import CoolProp._
- const PKG_ROOT  ·L16
- _imports_coolprop(tree: ast.AST) -> bool  ·L19
- test_no_coolprop_in_runtime_package()  ·L30

### tests/test_rho_u_inversion.py
_The (ρ, u) → T inversion and the fused properties_from_rho_u path._
- _u(T, rho)  ·L23
- @pytest.mark.parametrize test_roundtrip_under_1e8(label, Tlo, Thi, rlo, rhi)  ·L35
- test_fused_matches_properties()  ·L45 — properties_from_rho_u(ρ,u) == properties(T_recovered, ρ) elementwise.
- _fd(f, x, i, eps=1e-06)  ·L66
- @pytest.mark.parametrize test_inversion_gradients(T0, rho0)  ·L75
- test_properties_from_rho_u_differentiable()  ·L93 — grad through the fused path (e.g. dP/du) is finite and matches FD.

## Other source files

- bench/gpu_bench.sh  ·39 lines
